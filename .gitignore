.venv/
venv/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# data artefacts (regenerated by the pipeline; not versioned)
data/store/*.duckdb
data/raw/

# data/results/ IS versioned (unlike the rest of data/): the deployed API
# ships from this snapshot alone (Render has no free persistent disk), and
# it's small (~10MB). Re-run the pipeline + `export_price_history` and
# commit the diff to refresh it; see CLAUDE.md.

# R session files
.RData
.RDataTmp
.Rhistory

# editor / OS
.idea/
.vscode/
.DS_Store

# report deliverables: the text (results.md, model cards) is versioned;
# generated binaries are not
methodology.pdf
docs/figures/
# LaTeX build by-products
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz
# local ops center jobs (logs + metadata)
data/ops/
