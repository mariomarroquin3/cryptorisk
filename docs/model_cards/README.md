# Model cards

One card per model in `registry.all_models()`, auto-generated by `python -m cryptorisk.study.report`. Each card is the model's idea, its out-of-sample scorecard (coverage / ES / FZ0-MCS / density / QLIKE / decision layer) and its known limitations. The mathematics is in [`../methodology.tex`](../methodology.tex).
